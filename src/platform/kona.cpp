#include "platform/kona.h"
// KonaMap is entirely compile-time constants; this translation unit exists so
// the platform layer has a home for future runtime SoC helpers.
namespace hw::platform {} // namespace hw::platform
