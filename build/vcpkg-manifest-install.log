Detecting compiler hash for triplet x64-windows...
Compiler found: C:/Program Files/Microsoft Visual Studio/18/Community/VC/Tools/MSVC/14.51.36231/bin/Hostx64/x64/cl.exe
The following packages are already installed:
    bzip2[core,tool]:x64-windows@1.0.8#6
    capstone[core,arm64]:x64-windows@5.0.9
    liblzma:x64-windows@5.8.3
    unicorn:x64-windows@2.1.4
  * vcpkg-cmake:x64-windows@2025-08-07
  * vcpkg-cmake-config:x64-windows@2026-07-21
    zlib:x64-windows@1.3.2#2
The package bzip2 is compatible with built-in CMake targets:

    find_package(BZip2 REQUIRED)
    target_link_libraries(main PRIVATE BZip2::BZip2)

capstone provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(capstone CONFIG REQUIRED)
  target_link_libraries(main PRIVATE capstone::capstone)

capstone provides pkg-config modules:

  # Capstone disassembly engine
  capstone

liblzma is compatible with built-in CMake targets:

    find_package(LibLZMA REQUIRED)
    target_link_libraries(main PRIVATE LibLZMA::LibLZMA)

liblzma provides CMake targets:

    find_package(liblzma CONFIG REQUIRED)
    target_link_libraries(main PRIVATE liblzma::liblzma)

unicorn provides pkg-config modules:

  # Unicorn emulator engine
  unicorn

zlib is compatible with built-in CMake targets:

  find_package(ZLIB REQUIRED)
  target_link_libraries(main PRIVATE ZLIB::ZLIB)

zlib provides pkg-config modules:

  # zlib compression library
  zlib

All requested installations completed successfully in: 261 us
